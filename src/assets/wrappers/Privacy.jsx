import styled from 'styled-components';

const Wrapper = styled.div`
  .privacy-list {
    margin: 1rem 0;
  }

  .privacy-list li {
    padding: 0.4rem 0;
    line-height: 1.5;
  }
  P {
    line-height: 1.5;
  }
`;

export default Wrapper;
